serviceAccount:
  create: true
  name: external-dns
  annotations:
    eks.amazonaws.com/role-arn: ${external_dns_role_arn}

provider:
  name: aws

aws:
  region: ${region}
  zoneType: public

domainFilters:
  - ${domain}

policy: sync

txtOwnerId: ${cluster_name}

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi
