---
# ansible/roles/sql-server/meta/main.yml

galaxy_info:
  author: AnvilOps
  company: AnvilOps
  description: SQL Server pre-requisites and post-install configuration for AnvilOps-managed servers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Windows
      versions:
        - "2019"
        - "2022"
  galaxy_tags:
    - sql-server
    - database
    - windows
    - anvilops

dependencies:
  - role: common
  - role: windows-hardening
