---
# AnvilOps Ansible requirements
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - name: amazon.aws
    version: ">=7.0.0"
  - name: community.general
    version: ">=9.0.0"
  - name: community.windows
    version: ">=2.0.0"
  - name: microsoft.ad
    version: ">=1.5.0"
  - name: ansible.windows
    version: ">=2.0.0"
  - name: chocolatey.chocolatey
    version: ">=1.5.0"
